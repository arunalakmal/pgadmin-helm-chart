apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "swdpgadmin.fullname" . }}-test-connection"
  labels:
    {{- include "swdpgadmin.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "swdpgadmin.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
